user infinite scroll:
cache retrieved info in client side, in server side user skip; Then merge whole user array. or dymically append';
right now it's refetching everything





user profile:

{
	isOnline:
	location:
	selfIntro:
	picture:
	views:
	loginTimes:
	receiveFlirtTimes:
	sendFlirtTimes:
	messageTimes:
	activeIndex: (every not login day then minus 1, login once plus 1)
	tags: []
}



// login and logout callback

    Meteor.autorun(function () {
        if (Meteor.userId()) {

        } else {

        }
    });

// avatar option

    Avatar.options = {
        defaultImageUrl: "images/user.png"
    };